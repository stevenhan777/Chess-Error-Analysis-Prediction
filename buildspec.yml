version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Build complete"

artifacts:
  files:
    - application.py
    - requirements.txt
    - Procfile
    - "src/**/*"
    - "templates/**/*"
    - ".ebextensions/**/*"
  discard-paths: no